apiVersion: v2
name: rancher-stack
description: Rancher com publicação opcional por HTTPRoute e TLS terminado no Gateway
type: application
version: 0.1.0
appVersion: "1.0"
dependencies:
  - name: rancher
    # Faixa de exemplo. Depois de copiar, escolha uma versão testada e versione Chart.lock.
    version: "~2.14.0"
    repository: https://releases.rancher.com/server-charts/stable
